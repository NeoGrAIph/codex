diff --git a/codex-rs/tui/src/bottom_pane/footer.rs b/codex-rs/tui/src/bottom_pane/footer.rs
index 4893f2f6a..6bcb909ca 100644
--- a/codex-rs/tui/src/bottom_pane/footer.rs
+++ b/codex-rs/tui/src/bottom_pane/footer.rs
@@ -35,6 +35,7 @@
 use crate::key_hint;
 use crate::key_hint::KeyBinding;
 use crate::render::line_utils::prefix_lines;
+use crate::status::context_left_label_spans;
 use crate::status::format_tokens_compact;
 use crate::ui_consts::FOOTER_INDENT_COLS;
 use crossterm::event::KeyCode;
@@ -73,9 +74,7 @@ pub(crate) struct FooterProps {
 #[derive(Clone, Copy, Debug, Eq, PartialEq)]
 pub(crate) enum CollaborationModeIndicator {
     Plan,
-    #[allow(dead_code)] // Hidden by current mode filtering; kept for future UI re-enablement.
     PairProgramming,
-    #[allow(dead_code)] // Hidden by current mode filtering; kept for future UI re-enablement.
     Execute,
 }
 
@@ -656,6 +655,7 @@ fn shortcut_overlay_lines(state: ShortcutsState) -> Vec<Line<'static>> {
     let mut edit_previous = Line::from("");
     let mut quit = Line::from("");
     let mut show_transcript = Line::from("");
+    let mut show_agents = Line::from("");
     let mut change_mode = Line::from("");
 
     for descriptor in SHORTCUTS {
@@ -671,6 +671,7 @@ fn shortcut_overlay_lines(state: ShortcutsState) -> Vec<Line<'static>> {
                 ShortcutId::EditPrevious => edit_previous = text,
                 ShortcutId::Quit => quit = text,
                 ShortcutId::ShowTranscript => show_transcript = text,
+                ShortcutId::ShowAgents => show_agents = text,
                 ShortcutId::ChangeMode => change_mode = text,
             }
         }
@@ -692,6 +693,9 @@ fn shortcut_overlay_lines(state: ShortcutsState) -> Vec<Line<'static>> {
     }
     ordered.push(Line::from(""));
     ordered.push(show_transcript);
+    if show_agents.width() > 0 {
+        ordered.push(show_agents);
+    }
 
     build_columns(ordered)
 }
@@ -745,8 +749,8 @@ fn build_columns(entries: Vec<Line<'static>>) -> Vec<Line<'static>> {
 
 pub(crate) fn context_window_line(percent: Option<i64>, used_tokens: Option<i64>) -> Line<'static> {
     if let Some(percent) = percent {
-        let percent = percent.clamp(0, 100);
-        return Line::from(vec![Span::from(format!("{percent}% context left")).dim()]);
+        let spans = context_left_label_spans(percent, " context left", true, true);
+        return Line::from(spans);
     }
 
     if let Some(tokens) = used_tokens {
@@ -754,7 +758,7 @@ pub(crate) fn context_window_line(percent: Option<i64>, used_tokens: Option<i64>
         return Line::from(vec![Span::from(format!("{used_fmt} used")).dim()]);
     }
 
-    Line::from(vec![Span::from("100% context left").dim()])
+    Line::from(context_left_label_spans(100, " context left", true, true))
 }
 
 #[derive(Clone, Copy, Debug, Eq, PartialEq)]
@@ -769,6 +773,7 @@ enum ShortcutId {
     EditPrevious,
     Quit,
     ShowTranscript,
+    ShowAgents,
     ChangeMode,
 }
 
@@ -943,6 +948,15 @@ const SHORTCUTS: &[ShortcutDescriptor] = &[
         prefix: "",
         label: " to view transcript",
     },
+    ShortcutDescriptor {
+        id: ShortcutId::ShowAgents,
+        bindings: &[ShortcutBinding {
+            key: key_hint::ctrl(KeyCode::Char('n')),
+            condition: DisplayCondition::WhenCollaborationModesEnabled,
+        }],
+        prefix: "",
+        label: " to view agents",
+    },
     ShortcutDescriptor {
         id: ShortcutId::ChangeMode,
         bindings: &[ShortcutBinding {
