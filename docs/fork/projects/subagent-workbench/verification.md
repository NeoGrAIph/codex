# Subagent Workbench verification

## Required checks

| Surface | Required verification |
| --- | --- |
| Runtime source map | Focused core tests around MAv2 `spawn`, `list_agents`, `thread_note`, cwd and lifecycle status. |
| App-server projection | `thread/read`, `thread/list`, loaded thread list and `ThreadStatusChanged` tests if new fields or status handling are added. |
| TUI list/detail | `codex-tui` snapshots for the implemented `/agent` picker detail and the separate SAW dashboard view; future snapshots for empty, running path-backed MAv2, waiting, completed/interrupted/error, replay-only and narrow-width states. |
| Privacy/boundedness | Tests proving command output, raw reasoning, encrypted messages and long prompts are bounded or hidden. |
| Lifecycle actions | Positive/negative tests for select/watch, queue-only `send-message`, trigger-turn `follow-up`, `interrupt one` and `close one`; root/subtree/sibling/self ownership must be explicit. Deferred actions require contract tests before UI enablement. |
| Docs | `git diff --check -- docs/fork/features/subagent-workbench.md docs/fork/projects/subagent-workbench docs/fork/research/0.140.0/openclaude-agent-ux-runtime*.md`. |

## Scenario matrix

| Scenario | Expected result |
| --- | --- |
| No sub-agents | Workbench shows an empty state without changing `/agent` command semantics. |
| Two running MAv2 agents | Rows show stable labels, status, bounded current activity and selectable thread targets. |
| Waiting on approval/user input | Status derives from app-server `ThreadStatus::Active` flags and does not masquerade as idle. |
| Completed/interrupted/errored | Rows remain stable enough for replay/detail; actions are limited according to lifecycle contract. |
| Role template spawn | Row/detail show role/nickname from native metadata without duplicating the role registry. |
| Role/profile policy hydration | Detail shows bounded tool/action policy summaries from persisted `ThreadSpawn.tool_selection` and `ThreadSpawn.action_policy`, without reading live role files or creating a workbench policy registry. |
| Thread note present | Note appears as bounded recovery clue. Missing note renders empty state. |
| Different cwd | Detail shows cwd/workdir if available; no permission or workspace root widening occurs. |
| Loaded session model/reasoning | Detail shows model, reasoning effort and service tier only when the current TUI has native `ThreadSessionState`; replay-only rows do not guess those values. |
| Token usage notification | Detail shows bounded token anchors from the latest per-thread `ThreadTokenUsageUpdated`; missing usage renders no token line. |
| Encrypted or unavailable task message | Detail shows a controlled placeholder, not decrypted or guessed content. |
| Long output/reasoning | UI shows bounded summaries and never dumps raw output/reasoning. |
| `/agents` command | Still opens role template management, not the workbench. |

## Team/status scenario matrix

| Scenario | Expected result | Required evidence before implementation |
| --- | --- | --- |
| Footer/team indicator | Compact count/status hint derives from native tracked sub-agent threads, not from a team file. | Implemented as `/agent` header summary, not footer. Covered by `agent_picker_summary_counts_sub_agent_statuses` and workbench snapshots; no team file or parallel registry is added. |
| Running status | Row/status layer reports running from `ThreadStatus::Active` or live active turn state. | Covered by existing `agent_picker` focused run and workbench snapshot for running path-backed MAv2 agent. |
| Waiting on approval | Waiting approval is distinct from running/idle. | Covered by `thread_status_changed_updates_agent_picker_status_cache` and `agent_picker_status_labels_cover_waiting_and_error_states`. |
| Waiting on user input | Waiting user input is distinct from running/idle and parent `wait_agent`. | Covered by `agent_picker_status_labels_cover_waiting_and_error_states` and `thread_status_changed_updates_agent_picker_status_cache`, including `ThreadActiveFlag::WaitingOnUserInput` notification-cache regression coverage. |
| Idle loaded thread | Loaded idle thread renders as idle, not historical completed. | Loaded-thread status test plus snapshot. |
| System error | Error status takes precedence over stale activity summaries. | Status derivation unit test and snapshot/error row copy. |
| Not loaded or replay-only | Row remains watchable when a thread id is available but destructive actions stay disabled. | Snapshot for replay-only/closed row and disabled action reason. |
| Historical final annotation | Completed/interrupted/shutdown activity can annotate detail but does not override live loaded status. | Unit test combining final activity with live status. |
| Role labels as team roles | Planner/implementer/verifier labels come from `agent_role` and custom role templates, not hardcoded enums. | Spawn/list test with custom role names. |
| No mailbox/task-file semantics | UI does not show unread/read, busy/owner or OpenClaude task-file state unless a native Codex source is added. | Code review plus absence of new parallel storage/config. |

## Lifecycle action scenario matrix

| Scenario | Expected result | Required evidence before implementation |
| --- | --- | --- |
| View row detail | Detail opens for live, completed and replay-only rows without changing runtime state. | TUI snapshot for selected detail in live and closed/replay-only states. |
| Watch/select target | `SelectAgentThread` changes the displayed thread and preserves stable picker order. | Existing picker tests plus snapshot showing selected/current marker and footer copy. |
| Workbench read model | Normal `/agent` rows are derived through one ephemeral read model over `AgentNavigationState` and bounded per-thread summaries; the model does not execute actions, persist hidden state or call app-server. | Pure `codex-tui` unit test for summary/current/search/detail projection plus unchanged picker snapshots. |
| Explicit connect/switch copy | Normal row selection uses existing `SelectAgentThread`; selected detail shows `Connection:` and context-aware `Enter:` copy for current vs non-current rows, without adding runtime connection state. | Focused formatter/read-model test plus workbench snapshot. |
| Ctrl+T transcript-to-Agent-Window parity | First `Ctrl+T` remains native transcript overlay; repeated `Ctrl+T` while transcript overlay is active closes transcript and opens the separate SAW dashboard through `open_subagent_workbench`. | Focused TUI key decision test plus unchanged `/agent` picker snapshot and dedicated `subagent_workbench_dashboard` snapshot. |
| Select missing metadata row | Row with missing `agent_path`, role, note or cwd remains watchable if the thread id is native and replayable. | Formatter/unit test for empty fields and snapshot for controlled empty labels. |
| Stable created/updated timestamps | Native `Thread.created_at` and `Thread.updated_at` render as absolute UTC `Created:`/`Updated:` anchors when available; missing or zero timestamps render no line. | Covered by `agent_picker_selected_description_includes_workbench_detail` for deterministic UTC rendering and by the native `hydrate_agent_navigation_from_thread` path that copies `thread/read` metadata into `AgentNavigationState`; snapshot coverage remains tied to populated picker detail rather than a separate elapsed-age UI. |
| Role/profile policy summaries | Native `ThreadSpawn.tool_selection` and `ThreadSpawn.action_policy` snapshots render as bounded `Tool policy:` and `Action policy:` inspect anchors. Missing snapshots render no line; displayed summaries are not enforcement authority. | Covered by `agent_picker_selected_description_includes_workbench_detail` and `inactive_thread_started_notification_initializes_replay_session`. |
| Prompt/context preview | When native app-server `Thread.preview` is available, selected detail shows a bounded `Prompt:` line; receiver-matched buffered `CollabAgentToolCall.prompt` events render bounded `Context:` lines. Missing preview/context renders no line. | Formatter/unit test for whitespace normalization, receiver matching and raw-message privacy, plus snapshot coverage through existing picker detail. Do not use `thread/read(includeTurns=true)` or raw child `UserMessage` items. |
| Plan progress anchor | Latest per-thread `TurnPlanUpdated` notification renders bounded `Plan:` completed/total plus current in-progress step. | Covered by formatter test and `agent_picker_workbench_snapshot`; explanation, raw `ThreadItem::Plan`, `PlanDelta`, transcript turns and command/reasoning output are not used. |
| Send-message from workbench | Root or path-backed subtree-owner workbench can open `Send message` for one eligible path-backed descendant, enter message text and queue it without starting a turn. Main/root/pathless target/pathless owner/self/sibling/closed/error targets are denied. | Covered locally by `thread_manager_agent_message_send_queues_without_triggering_turn`, `thread_manager_agent_message_send_allows_subtree_owner_descendant`, `serialize_agent_message_send`, schema fixture tests, `agent_picker_workbench_root_actions`, `agent_picker_send_message_prompt`, `agent_picker_interrupt_confirmation`, `agent_picker_interrupt_child_context_snapshot` snapshots and `agent_picker_message_preflight_allows_subtree_owner_descendants_only` for preflight boundaries. |
| Followup from workbench | Root or path-backed subtree-owner workbench can open `Follow up` for one idle eligible path-backed descendant, enter next-task text and explicitly confirm a trigger-turn follow-up. Main/root/pathless target/pathless owner/self/sibling/running/waiting/error/closed targets are denied. | Covered locally by `thread_manager_agent_followup_starts_trigger_turn`, `thread_manager_agent_followup_allows_subtree_owner_descendant`, `serialize_agent_followup_send`, schema fixture tests, `agent_picker_followup_prompt_and_confirmation_snapshot`, `agent_picker_followup_preflight_allows_subtree_owner_idle_descendants_only` and `agent_picker_interrupt_child_context_snapshot`. The snapshots distinguish queue-only `Send message` from `Follow up` / `starts a turn`, and child-context rendering shows `Follow up` only for an idle descendant, not for the owner or sibling. |
| Interrupt one from root | Root can interrupt one spawned non-root target after confirmation; confirmation/success copy shows the cached pre-interrupt status, the thread stays available and row status refreshes from app-server state. | Covered locally by `agent_picker_interrupt_action_and_confirmation_snapshot` for root-visible action row, confirmation copy and cached current-status display; `agent_picker_interrupt_confirmed_refreshes_target_status` verifies post-action liveness refresh. This uses TUI cached status and does not require app-server `turn/interrupt` to return a previous-status payload. |
| Interrupt one from subtree owner | Non-root agent can interrupt a descendant in its own `AgentPath` subtree. | Covered locally by `agent_picker_interrupt_child_context_snapshot`: child-context picker renders the interrupt action for the descendant and does not render self or sibling interrupt actions. |
| Interrupt root/self/sibling/ancestor | Action is denied or disabled with controlled diagnostic. | Covered locally by `agent_picker_interrupt_preflight_denies_self_and_missing_path` and `agent_picker_interrupt_preflight_enforces_status_and_path_ownership`: self, missing path, root target, sibling/outside subtree, idle, error and closed targets are denied; descendant target is allowed. |
| Wait/result observation | Workbench shows waiting/timed-out/completed signal as bounded status; no raw transcript/result-file dump. | Core wait-agent timeout/mailbox tests plus TUI snapshots for waiting and completed indicators. |
| Close one | Root/subtree workbench can open `Close <agent>` for one owned path-backed spawned descendant after destructive confirmation; target spawn edge is marked closed, live target subtree is shut down and the thread remains available for replay/resume. Main/root/self/pathless/already-closed/outside-subtree targets are denied. | Covered locally by `thread_manager_agent_close_closes_path_backed_descendant`, `serialize_agent_close`, schema fixture tests, `agent_picker_interrupt_action_and_confirmation_snapshot`, `agent_picker_interrupt_child_context_snapshot`, `agent_picker_close_preflight_enforces_status_and_path_ownership` and `cargo insta pending-snapshots --workspace-root .` with no pending snapshots after close confirmation acceptance. |
| Dismiss row | Owned path-backed spawned descendant can be hidden from Agent Window without closing/unloading/archive/delete side effects. | Covered locally by `update_thread_metadata_sets_agent_hidden_on_rollout`, `thread_manager_agent_dismiss_marks_visibility_without_shutdown`, `agent_dismiss_requires_experimental_api_capability`, `read_model_excludes_hidden_agents_from_workbench_rows`, schema fixture regeneration and no pending snapshots. |
| Retry seed | New MAv2 `spawn_agent` threads persist the original task message in native `SessionSource::SubAgent(ThreadSpawn.initial_task)`; old sessions and legacy spawn/resume paths keep it absent. | Covered by `multi_agent_v2_spawn_returns_path_and_send_message_accepts_relative_path` assertion on the child thread config snapshot and test-target compile coverage for old fixtures with `initial_task: None`. |
| Retry row | Root/subtree workbench can open `Retry <agent>` for an owned completed/interrupted/errored path-backed descendant with durable `initial_task`; confirmation spawns a fresh sibling and keeps the original unchanged. Missing seed, root, self, pathless, outside-subtree, running and waiting targets are denied. | Covered by `thread_manager_agent_retry_spawns_fresh_sibling_from_initial_task`, `serialize_agent_retry`, schema fixture tests, `agent_retry_requires_experimental_api_capability`, `agent_picker_retry_preflight_allows_subtree_owner_idle_or_error_descendants_only`, `agent_picker_retry_child_context_snapshot`, `agent_picker_followup_prompt_and_confirmation_snapshot` and no pending snapshots. |
| Stop all | Root/subtree workbench can open `Stop all agents` when at least one owned visible descendant is live or waiting; confirmation closes only those visible live descendants and leaves hidden/idle/completed/error/closed rows unchanged. Server returns affected/stopped/failed counts and per-affected-target failure reasons, and TUI reports partial failure as a non-atomic result. | Covered by `thread_manager_agent_stop_all_closes_live_descendants_in_scope`, `agent_stop_all_target_result_records_partial_failures`, `thread_manager_agent_stop_all_rejects_pathless_non_root_author`, `serialize_agent_stop_all`, schema fixture tests, `agent_stop_all_requires_experimental_api_capability`, `agent_picker_stop_all_targets_live_root_and_subtree_descendants_only`, `agent_stop_all_user_copy_reports_zero_success_and_partial_failure`, root action snapshot, subtree confirmation copy and `agent_picker_stop_all_confirmation`. Partial-failure aggregation records successful target descendants and expands a failed top-level close into failures for every affected descendant under that target; TUI copy reports `Stopped <n> of <m> agents` plus failed-count hint instead of hiding the partial result. |

## Confirmation and snapshot requirements

| Action | Confirmation | Snapshot requirement |
| --- | --- | --- |
| `view` / `watch` / `select` | None. | Picker detail and selected/current row snapshots. |
| `interrupt one` | `Interrupt this agent?` with target subtitle `current status: <status>` and action copy `Current status: <status>. Stop the current turn only. It does not close or delete the thread.` | Implemented confirmation modal and root-view action-row snapshots; disabled action reasons are covered by focused preflight tests for self, missing path, root target, sibling/outside subtree and unavailable statuses. |
| `send-message` | `Message agent` prompt with target context, action copy `queue message only`, success copy `Message queued for <agent>.`, and empty input diagnostic `Cannot send an empty agent message.` | Implemented root/subtree-owner action rows and prompt snapshots; core queue-only tests assert encrypted `InterAgentCommunication { trigger_turn: false }` from root and path-backed subtree-owner authors; protocol/schema tests cover `agent/message/send`; preflight tests cover descendant-only, path-backed-author, self/sibling/root/pathless/closed/error rejection. |
| `followup_task` | `Follow up with agent` prompt followed by `Start a follow-up turn?`, action row `Start follow-up`, copy `Queue an encrypted follow-up and start this agent when capacity is available.`, success copy `Follow-up started for <agent>.`, and hint `The agent may request approvals under its existing sandbox.` | Implemented root/subtree-owner action rows, prompt and confirmation snapshots; core trigger-turn tests assert encrypted `InterAgentCommunication { trigger_turn: true }` from root and path-backed subtree-owner authors; protocol/schema tests cover `agent/followup/send`; preflight tests cover idle-only, descendant-only, path-backed-author, self/sibling/root/pathless/closed/error/running/waiting rejection. |
| `close one` | `Close this agent?` with target subtitle `current status: <status>` and action copy `Current status: <status>. Mark this spawn edge closed and shut down the live target and descendants.` | Implemented destructive modal/action-row snapshots and preflight tests for self, main/root, missing path, already closed and outside-subtree cases; runtime test verifies native `AgentControl::close_agent` receives the shutdown path through `ThreadManager::close_agent_from_workbench`. |
| `retry` | `Retry this agent?` with target subtitle `current status: <status>` and action copy `Current status: <status>. Spawn a fresh sibling from the stored initial task.` | Implemented confirmation snapshot plus root/subtree action-row snapshots; runtime test verifies fresh sibling spawn from durable `initial_task`, original unchanged and stored task submitted through native spawn. |
| `stop all` | `Stop all agents?` with subtitle `{scope} · {count} live agents` and action copy `Stop {count} running/waiting agents in this scope. Idle, completed and errored agents are unchanged.` | Implemented root action row, subtree target/preflight coverage and destructive confirmation snapshot. The list row is intentionally compact (`{scope} · {count} live · bulk close`) so per-agent `Interrupt`/`Close` remain visible in compact Agent Window snapshots. |

## Implemented verification

| Check | Result |
| --- | --- |
| `just test -p codex-tui agent_picker thread_status_changed_updates_agent_picker_status_cache inactive_thread_started_notification_preserves_primary_model_when_path_missing agent_status open_agent_picker` | Passed: 24/24 tests on 2026-06-18; pure formatter tests, picker liveness tests, status-cache notification, metadata hydration, legacy status-feed summaries and workbench snapshots cover scan-first row summary, selected detail, derived waiting/error status labels, bounded recent activity, root-view interrupt action row, confirmation snapshot, self/missing-path/root/sibling/status preflight negatives and descendant allow case. |
| `just test -p codex-app-server-protocol serialize_agent_message_send typescript_schema_fixtures_match_generated json_schema_fixtures_match_generated`; `just test -p codex-core thread_manager_agent_message_send_queues_without_triggering_turn`; `just test -p codex-tui agent_picker_interrupt_action_and_confirmation_snapshot agent_picker_message_preflight_allows_root_workbench_descendants_only`; `cargo insta pending-snapshots --workspace-root .` | Passed on 2026-06-18; historical root-workbench baseline covering typed `agent/message/send`, schema fixture sync, queue-only encrypted `InterAgentCommunication { trigger_turn: false }`, root-workbench action/prompt snapshots (`agent_picker_workbench_root_actions`, `agent_picker_send_message_prompt`, `agent_picker_interrupt_confirmation`) and no pending snapshots. |
| `just test -p codex-tui inactive_thread_started_notification_initializes_replay_session open_agent_picker_marks_loaded_threads_open agent_picker_selected_description_includes_workbench_detail agent_picker_workbench_snapshot` | Passed: 4/4 tests on 2026-06-18; verifies session-backed model/provider hydration for `ThreadStarted`, no guessed model for loaded `thread/read` rows, formatter detail copy for model/reasoning/service tier and unchanged workbench snapshot. |
| `just test -p codex-tui agent_picker open_agent_picker thread_status_changed_updates_agent_picker_status_cache inactive_thread_started_notification_preserves_primary_model_when_path_missing` | Passed: 22/22 tests on 2026-06-18; broader picker/open-agent regression pass after session-backed model/reasoning hydration. |
| `just test -p codex-tui agent_picker_token_usage_summary_is_bounded_and_optional agent_picker_selected_description_includes_workbench_detail agent_picker_workbench_snapshot` | Passed: 3/3 tests on 2026-06-18; verifies bounded token summary formatting, optional empty usage behavior and snapshot coverage for the selected-detail `Tokens:` anchor. |
| `just test -p codex-tui agent_picker_interrupt_confirmed_refreshes_target_status agent_picker_interrupt_action_and_confirmation_snapshot agent_picker_interrupt_preflight_denies_self_and_missing_path agent_picker_interrupt_preflight_enforces_status_and_path_ownership` | Passed: 4/4 tests on 2026-06-18; verifies confirmed interrupt still uses existing TUI/app-server thread op path, shows cached current/pre-interrupt status in confirmation copy and refreshes the picker row through app-server liveness after success. |
| `just test -p codex-tui agent_picker_selected_description_includes_workbench_detail agent_picker_status_labels_cover_waiting_and_error_states agent_picker_workbench_snapshot agent_picker_interrupt_action_and_confirmation_snapshot` | Passed: 4/4 tests on 2026-06-18 after accepting expected snapshots; verifies selected detail separates `Inspect` recovery anchors from `Actions` while preserving the existing watch action and interrupt action row/confirmation path. |
| `just test -p codex-tui agent_picker_selected_description_includes_workbench_detail agent_picker_workbench_snapshot open_agent_picker_marks_loaded_threads_open inactive_thread_started_notification_preserves_primary_model_when_path_missing agent_picker_interrupt_action_and_confirmation_snapshot` | Passed: 5/5 tests on 2026-06-18; verifies stable `Created`/`Updated` detail formatting, embedded thread metadata hydration with positive dynamic timestamps, existing workbench snapshot, model/session metadata preservation and interrupt action snapshot. |
| `just test -p codex-tui agent_picker_selected_description_includes_workbench_detail agent_picker_status_labels_cover_waiting_and_error_states inactive_thread_started_notification_initializes_replay_session agent_picker_workbench_snapshot` | Passed: 4/4 tests on 2026-06-18 after accepting expected snapshot; verifies `Thread.preview` hydrates into the picker cache, renders as a bounded `Prompt:` inspect anchor and keeps session metadata hydration intact. |
| `just test -p codex-tui agent_picker_prompt_context_matches_receiver_and_hides_raw_messages agent_picker_selected_description_includes_workbench_detail agent_picker_workbench_snapshot` | Passed: 3/3 tests on 2026-06-18 after accepting expected snapshot; verifies receiver-matched `CollabAgentToolCall.prompt` context, whitespace normalization, hidden raw `UserMessage`/other receiver prompts and selected-detail `Context:` rendering. |
| `just test -p codex-tui agent_picker_interrupt_child_context_snapshot` | Passed: 1/1 tests on 2026-06-18 after accepting expected snapshot; verifies a non-root active agent sees an interrupt action only for a descendant in its own `AgentPath` subtree, not for itself or a sibling. |
| `just test -p codex-tui agent_picker_plan_progress_summary_is_bounded_and_structured agent_picker_selected_description_includes_workbench_detail agent_picker_workbench_snapshot` | Passed: 3/3 tests on 2026-06-18 after accepting expected snapshot; verifies bounded structured `TurnPlanUpdated` progress anchor and that plan explanation/raw transcript surfaces are not rendered. |
| `just test -p codex-tui agent_picker_summary_counts_sub_agent_statuses agent_picker_workbench_snapshot agent_picker_interrupt_action_and_confirmation_snapshot` | Passed: 3/3 tests on 2026-06-18 after accepting expected snapshots; verifies `/agent` header summary counts sub-agent statuses from native picker cache, excludes the main thread and keeps the existing action rows visible under the summary. |
| `just test -p codex-tui thread_status_changed_updates_agent_picker_status_cache` | Passed on 2026-06-18; verifies app-server `ThreadStatusChanged` notifications update the workbench cache for both `WaitingOnApproval` and `WaitingOnUserInput` active flags. |
| `cargo insta pending-snapshots --workspace-root .` | Passed: no pending snapshots after acceptance. |
| `just test -p codex-app-server-protocol serialize_agent_close schema_fixtures_match_generated`; `just test -p codex-core thread_manager_agent_close_closes_path_backed_descendant thread_manager_agent_message_send_queues_without_triggering_turn close_agent_submits_shutdown_and_returns_previous_status`; `just test -p codex-tui agent_picker_interrupt_action_and_confirmation_snapshot agent_picker_interrupt_child_context_snapshot agent_picker_close_preflight_enforces_status_and_path_ownership`; `cargo insta pending-snapshots --workspace-root .` | Passed on 2026-06-18 after accepting the expected close action/confirmation snapshots; covers typed `agent/close`, schema fixture sync, native `AgentControl::close_agent` path, TUI close action rows, destructive confirmation and ownership/status preflight. |
| `just test -p codex-app-server-protocol serialize_agent_followup_send schema_fixtures_match_generated`; `just test -p codex-core thread_manager_agent_followup_allows_subtree_owner_descendant thread_manager_agent_followup_starts_trigger_turn thread_manager_agent_message_send_queues_without_triggering_turn multi_agent_v2_followup_task_completion_notifies_parent_on_every_turn`; `just test -p codex-tui agent_picker_followup_preflight_allows_subtree_owner_idle_descendants_only agent_picker_interrupt_child_context_snapshot agent_picker_followup_prompt_and_confirmation_snapshot agent_picker_interrupt_action_and_confirmation_snapshot`; `cargo insta pending-snapshots --workspace-root .` | Passed locally on 2026-06-18 after accepting the expected subtree-owner child-context action snapshot; covers typed `agent/followup/send`, schema fixture sync, native trigger-turn encrypted `InterAgentCommunication { trigger_turn: true }`, root/subtree-owner action row/prompt/confirmation snapshots, idle-only/descendant-only/path-backed-author preflight boundaries, no interrupt regression and no pending snapshots. |
| `just test -p codex-core thread_manager_agent_message_send_allows_subtree_owner_descendant thread_manager_agent_message_send_queues_without_triggering_turn thread_manager_agent_followup_allows_subtree_owner_descendant`; `just test -p codex-tui agent_picker_message_preflight_allows_subtree_owner_descendants_only agent_picker_interrupt_child_context_snapshot agent_picker_message_child_context_snapshot agent_picker_followup_preflight_allows_subtree_owner_idle_descendants_only`; `cargo insta pending-snapshots --workspace-root .` | Passed on 2026-06-18 after accepting expected child-context and compact message snapshots; Kepler rerun confirmed core 3/3, TUI 4/4, no pending snapshots and scoped `git diff --check`. Covers path-backed subtree-owner queue-only delivery, pathless non-root author rejection, sibling/self/root denial, child-context `Send message` action visibility, preservation of first-screen interrupt/close/follow-up actions and no follow-up regression. |
| `cargo check -p codex-tui --tests`; `just test -p codex-tui read_model_builds_summary_current_row_and_bounded_detail_projection agent_picker_workbench_snapshot agent_picker_workbench_root_actions agent_picker_interrupt_child_context_snapshot`; `cargo insta pending-snapshots --workspace-root .`; scoped `git diff --check` | Passed on 2026-06-18 for `feature/140/workbench-read-model`; verifies the new ephemeral read model compiles, preserves existing workbench snapshots and leaves no pending snapshot artifacts. |
| `just test -p codex-tui read_model_builds_summary_current_row_and_bounded_detail_projection agent_picker_selected_description_includes_workbench_detail agent_picker_status_labels_cover_waiting_and_error_states agent_picker_workbench_snapshot`; `cargo insta pending-snapshots --workspace-root codex-rs`; scoped `git diff --check` | Passed locally on 2026-06-18 after accepting the expected `agent_picker_workbench.snap` update; verifies `Connection:` and context-aware `Enter:` selected-detail copy over existing `SelectAgentThread`, with no pending snapshots. |
| `cargo check -p codex-app-server-protocol -p codex-thread-store -p codex-core -p codex-app-server -p codex-tui`; `just write-app-server-schema`; `just fmt`; focused dismiss tests delegated to Confucius: `just test -p codex-thread-store update_thread_metadata_sets_agent_hidden_on_rollout`, `just test -p codex-core thread_manager_agent_dismiss_marks_visibility_without_shutdown`, `just test -p codex-app-server-protocol schema_fixtures_match_generated`, `just test -p codex-app-server agent_dismiss_requires_experimental_api_capability`, `just test -p codex-tui read_model_excludes_hidden_agents_from_workbench_rows agent_picker_workbench_snapshot`, `cargo insta pending-snapshots --workspace-root .`, scoped `git diff --check` | Passed on 2026-06-18. Covers rollout `SessionMeta.agent_hidden`, thread-id scoped metadata replay, no shutdown side effect, experimental `agent/dismiss`, app-server `Thread.agentHidden` schema propagation and TUI read-model filtering. |
| Historical command before rename: `just test -p codex-tui ctrl_t_on_transcript_overlay_switches_to_agent_picker_path agent_picker_workbench_snapshot`; `cargo insta pending-snapshots --workspace-root codex-rs`; scoped `git diff --check`. Current test name: `repeated_ctrl_t_cycles_from_native_transcript_to_agent_window`. | Passed on 2026-06-18; Confucius confirmed 2/2 tests, no pending snapshots and clean scoped diff-check. Covers the transcript-overlay shortcut decision and preserves the existing workbench snapshot. |
| `cargo check --tests -p codex-protocol -p codex-thread-store -p codex-core -p codex-tui -p codex-app-server -p codex-app-server-protocol`; Confucius focused rerun: `cargo check -p codex-protocol -p codex-thread-store -p codex-core -p codex-tui -p codex-app-server`; `just test -p codex-core multi_agent_v2_spawn_returns_path_and_send_message_accepts_relative_path`; historical TUI command before rename: `just test -p codex-tui ctrl_t_on_transcript_overlay_switches_to_agent_picker_path agent_picker_workbench_snapshot`; `cargo insta pending-snapshots --workspace-root .` | Passed on 2026-06-19. Covers durable `ThreadSpawn.initial_task` test fixture compatibility, MAv2 spawn seed persistence, preserved transcript-to-Agent-Window shortcut behavior and no pending snapshots. |
| `just test -p codex-core thread_manager_agent_retry_spawns_fresh_sibling_from_initial_task`; `just test -p codex-app-server-protocol serialize_agent_retry schema_fixtures_match_generated`; `just test -p codex-app-server agent_retry_requires_experimental_api_capability`; `just test -p codex-tui agent_picker_followup_prompt_and_confirmation_snapshot agent_picker_interrupt_child_context_snapshot agent_picker_retry_child_context_snapshot agent_picker_retry_preflight_allows_subtree_owner_idle_or_error_descendants_only`; `cargo insta pending-snapshots --workspace-root .`; `codex-fork` release build smoke | Passed on 2026-06-19. Covers native retry through durable `initial_task`, fresh sibling spawn, original thread unchanged, typed experimental `agent/retry`, schema fixture sync, app-server experimental capability gating, root/subtree TUI action rows, retry confirmation snapshots and no pending snapshots. `codex-fork` built `codex-rs/target/release/codex`; the wrapper then exited with `stdin is not a terminal`, which is an interactive launch limitation rather than a build failure. |
| `just test -p codex-core thread_manager_agent_stop_all`; `just test -p codex-app-server-protocol serialize_agent_stop_all schema_fixtures_match_generated`; `just test -p codex-app-server agent_stop_all_requires_experimental_api_capability`; historical TUI command before rename: `just test -p codex-tui agent_picker_interrupt_action_and_confirmation_snapshot agent_picker_interrupt_child_context_snapshot agent_picker_stop_all_targets_live_root_and_subtree_descendants_only ctrl_t_on_transcript_overlay_switches_to_agent_picker_path`; `cargo insta pending-snapshots --workspace-root .` | Passed on 2026-06-19 after accepting expected root/child action and stop-all confirmation snapshots. Covers typed experimental `agent/stopAll`, schema fixture sync, server-side path-backed author rejection, live-descendant-only native close through `AgentControl::close_agent`, idle/completed/error/closed skip behavior, root/subtree TUI target selection, destructive confirmation copy and preserved repeated Ctrl+T transcript-to-Agent-Window behavior. |
| `just test -p codex-core thread_manager_agent_stop_all_closes_live_descendants_in_scope agent_stop_all_target_result_records_partial_failures thread_manager_agent_stop_all_rejects_pathless_non_root_author` | Passed 3/3 on 2026-06-19. Covers live-visible-descendant stop-all happy path, server-side exclusion of `agent_hidden=true` descendants from bulk close, pathless non-root author denial and the partial-failure aggregation contract: one top-level target success marks its descendants stopped, while a failed top-level close reports every affected descendant under that target in `failed` without discarding successful targets. |
| `just fmt`; `just test -p codex-tui agent_stop_all_user_copy_reports_zero_success_and_partial_failure agent_picker_stop_all_targets_live_root_and_subtree_descendants_only` | Passed 2/2 on 2026-06-19. Covers TUI-visible stop-all result branches for empty, full success and partial failure, including exact partial copy `Stopped <n> of <m> agents.` and failed-count retry hint, while preserving live visible target selection. |
| `just fmt`; `just test -p codex-tui repeated_ctrl_t_cycles_from_native_transcript_to_agent_window` | Passed on 2026-06-19 after renaming the Ctrl+T predicate/test to describe the then-current contract: first `Ctrl+T` remained native transcript overlay, repeated `Ctrl+T` while transcript was active opened the old Agent Window picker. The current contract is superseded by the 2026-06-20 SAW dashboard entry below. |
| `just fmt`; `just test -p codex-tui repeated_ctrl_t_cycles_from_native_transcript_to_agent_window`; `cargo insta pending-snapshots --workspace-root .`; scoped `git diff --check` for touched Ctrl+T files | Passed on 2026-06-19 after extracting the Ctrl+T decision into one helper: `OpenTranscript` when no transcript overlay is active, `OpenAgentWindow` when the native transcript overlay is active, and no action for plain `t`. Covers the requested additive behavior after native `open_transcript`, not a first-press replacement. |
| `just fmt`; `just test -p codex-tui agent_picker_selected_description_includes_workbench_detail inactive_thread_started_notification_initializes_replay_session agent_picker_workbench_snapshot read_model_builds_summary_current_row_and_bounded_detail_projection` | Passed 4/4 on 2026-06-19. Covers bounded `Tool policy:`/`Action policy:` selected-detail formatting, native `ThreadSpawn` snapshot hydration through inactive thread replay/session initialization, unchanged workbench read model and unchanged Agent Window snapshot. |
| `just fmt`; `just test -p codex-tui repeated_ctrl_t_cycles_from_native_transcript_to_agent_window agent_picker_workbench_snapshot`; `cargo insta pending-snapshots --workspace-root .`; scoped `git diff --check` for touched TUI/docs files | Passed on 2026-06-19. Historical checkpoint for tightened `Ctrl+T` routing before the separate SAW renderer: first `open_transcript` shortcut used native `open_transcript_overlay`, repeated `Ctrl+T` from active transcript overlay opened Agent Window through `open_agent_picker`, with no Agent Window snapshot churn and no pending snapshots. The current 2026-06-20 implementation routes repeated `Ctrl+T` to `open_subagent_workbench`. |
| `just fmt`; `just test -p codex-tui repeated_ctrl_t_cycles_from_native_transcript_to_agent_window agent_picker_retry_preflight_allows_subtree_owner_idle_or_error_descendants_only agent_picker_retry_child_context_snapshot agent_picker_followup_prompt_and_confirmation_snapshot agent_picker_workbench_snapshot agent_picker_stop_all_targets_live_root_and_subtree_descendants_only`; `cargo insta pending-snapshots --workspace-root .` | Passed 6/6 on 2026-06-19. Confirms repeated `Ctrl+T` still opens Agent Window only after native transcript, positive Retry actions require hydrated durable `ThreadSpawn.initial_task`, legacy rows without that seed are denied by TUI preflight instead of advertising a retry action that the backend would reject, and hidden running rows are excluded from TUI `stop all` target selection/count. |
| `just fmt`; `just test -p codex-tui repeated_ctrl_t_cycles_from_native_transcript_to_agent_window agent_picker_workbench_snapshot`; `cargo insta pending-snapshots`; `git diff --check -- codex-rs/tui/src docs/fork/features/subagent-workbench.md docs/fork/projects/subagent-workbench` | Passed on 2026-06-20. Confirms first `Ctrl+T` remains native transcript overlay, repeated `Ctrl+T` routes to separate `SubagentWorkbenchView`, `/agent` numbered picker snapshot remains unchanged, and SAW has dedicated `subagent_workbench_dashboard` snapshot with compact rows plus `Actions for <agent>:` and `Inspect:` blocks. |
| Confucius read-only retry audit: same focused core/protocol/app-server/TUI retry tests, `cargo insta pending-snapshots --workspace-root .`, `git diff --check`, and stale-doc wording sweep for `subagent-workbench` | PASS on 2026-06-19; no High/Medium findings. Confirms retry is no longer documented as disabled/deferred except for historical changelog text explicitly marked as an earlier checkpoint. |
| Rawls independent audit for `feature/140/workbench-read-model` | `PASS_WITH_LOW` on 2026-06-18; no High/Medium blockers. Low findings were to include the new untracked read-model file and update stale docs wording, both addressed before commit. |

## Known gaps after first slice

- `feature/140/workbench-read-model` adds a first-class ephemeral read model for normal `/agent` rows; action rows, app-server methods, lifecycle mutations, persistence and protocol/schema remain unchanged in that slice.
- Prompt/context preview is limited to app-server `Thread.preview` and receiver-matched buffered `CollabAgentToolCall.prompt`. Raw turns, child `UserMessage` items, rollout operations, other receivers and encrypted inter-agent content stay intentionally out of scope.
- Plan progress is limited to latest per-thread `TurnPlanUpdated` notification. Raw `ThreadItem::Plan`, `PlanDelta`, explanation text and historical transcript reconstruction stay intentionally out of scope.
- Stable `Created:`/`Updated:` thread timestamps are implemented from native thread metadata. Live elapsed age and current-work age remain unresolved because spawned age and active-turn age are different fields and would need a ticking UI contract.
- App-server top-level projection for `thread_note`/`agent_path` may be insufficient; avoid schema changes until current sources are proven inadequate.
- `interrupt one` is implemented for the first TUI slice with cached pre-interrupt status copy and post-action liveness refresh. It intentionally does not add an app-server previous-status payload; the displayed previous status is the TUI cache value before submitting `turn/interrupt`.
- Queue-only `send-message` is implemented for root/subtree-owner eligible path-backed descendants through `agent/message/send` and `ThreadManager::queue_inter_agent_message`; pathless non-root owners, self, siblings, root, error and closed targets remain denied. Trigger-turn `follow-up` is implemented for root/subtree-owner workbench through `agent/followup/send` and `ThreadManager::send_inter_agent_followup`; pathless non-root owners, self, siblings, root and unavailable targets remain denied. `close one` is implemented through `agent/close` and native `AgentControl::close_agent`; non-destructive `dismiss` is implemented through `agent/dismiss` and rollout `SessionMeta.agent_hidden`; `retry` is implemented through `agent/retry`, durable `ThreadSpawn.initial_task` and native fresh-sibling spawn; `stop all` is implemented as scoped native close for live descendants only, with partial-failure reporting.
- Token detail is limited to latest per-thread `ThreadTokenUsageUpdated`; historical replay without token notifications and account-wide usage are intentionally out of scope for the workbench detail. Current slices add derived status, `thread_note`, cwd, model provider, session-backed model/reasoning/service tier, token usage anchors and bounded activity from native thread metadata, `ThreadSessionState` and buffered notifications/items.
- `stop all` intentionally does not stop hidden/idle/completed/error/closed rows and does not perform cross-root process killing; use individual close/dismiss/retry actions or native thread history/read/resume paths for non-live or hidden rows.
- `dismiss` has no first-slice undismiss/reveal UX. Hidden agents remain recoverable through native thread history/read/resume paths; `thread/archive` remains intentionally separate.
- Direct first-press `Ctrl+T` SAW takeover is intentionally not the target behavior: first press remains the native transcript overlay, and the implemented parity is repeated `Ctrl+T` from transcript overlay to the separate SAW dashboard, not to `/agent`.
