# Fork feature ledger

Этот каталог фиксирует пользовательские возможности, которые были добавлены, перенесены или доработаны в fork-ветках. Основная единица учета здесь - не отдельная ветка и не отдельный commit subject, а пользовательская возможность: что стало доступно пользователю, в каких ветках это реализовывалось, и какими коммитами.

## Методика

- Источники: локальные `fork/*`, `origin/fork/*`, `chrome_plugin`, `origin/chrome_plugin`, а также явно помеченный текущий dirty state, если feature ещё не закоммичена.
- Upstream release commits и upstream PR commits используются как baseline/evidence и не считаются fork-возможностями сами по себе.
- Если один функционал переносился между release-ветками, он описан в одном feature doc с несколькими ветками в таблице коммитов.
- Если назначение commit не установлено из subject, diff или существующих docs/tests, это помечается как `не установлено локально`.

## Возможности

| Возможность | Пользовательский смысл | Статус | Основные ветки |
| --- | --- | --- | --- |
| [Subagent Workbench / agents overlay](subagent-workbench.md) | TUI-окно для просмотра sub-agents, их статусов и контекста. Польза: легче управлять параллельной работой и понимать, кто чем занят. | Fork/140 `/agent` workbench slice expanded with scan-first rows, bounded detail anchors, status cache, repeated `Ctrl+T` from native transcript overlay, confirmed `interrupt one`, root/subtree-owner `send-message`/`follow-up`, confirmed `close one`, non-destructive `dismiss`, native fresh-sibling `retry`, scoped destructive `stop all`, explicit connect/switch copy and bounded role/profile hydration from native thread/session snapshots | `fork/saw`, `fork/101`, `fork/106`, `fork/107`, `fork/118`, `fork/130`, `chrome_plugin`, `fork/140` |
| [Interactive collaboration mode](interactive-collaboration-mode.md) | Режим интерактивного взаимодействия с явной индикацией в TUI. Польза: пользователю проще взаимодействовать с агентом ввиду возможности интерактивного выбора из предложенных вариантов. | Реализовано локально на `fork/140` через native `ModeKind::Interactive`, `/interactive`, footer indicator, schema and `request_user_input` gating | `fork/101`, `fork/140` |
| [Agent role templates](agent-role-templates.md) | Шаблоны ролей для sub-agents: explorer, worker, reviewer и т.п. Польза: agents стартуют с понятной специализацией и не требуют каждый раз ручного описания роли. | Fork/140 native TUI iteration expanded with TOML wizard, source states, field-source summary, per-field runtime TOML provenance, read-only capability detail, `/agent-roles` import entrypoint to compile markdown/frontmatter agents into native TOML, config-backed built-in `reviewer` with native read-only defaults, catalog-backed model/provider/reasoning picker controls and staged current-model/current model-provider/current-reasoning/current-tools controls over the native TOML draft/update path; native `rust-v0.140.0` covered this partially | `fork/saw`, `fork/106`, `fork/107`, `fork/111`, `fork/118`, `fork/130`, `chrome_plugin`, `fork/140` |
| [Agent role tool selection](agent-role-tool-selection.md) | Ограничение набора tools для sub-agent role через native Codex tool planning. Польза: роли получают понятные capability boundaries без расширения permissions/sandbox. | Runtime allow/deny slice, enforced-plan stale-id diagnostics, planner-owned catalog projection with selected state/exposure metadata, thread-scoped app-server catalog read, read-only role detail projection, native TOML draft authoring, catalog-assisted create/edit flow, whole-policy `agentRole/toolSelection/set` and path-backed retention through `ThreadSpawn.tool_selection` implemented in `fork/140`; partial patch/versioned writes and external `config_file` writes remain outside the contract | `fork/140` |
| [Thread notes](thread-notes.md) | Короткие заметки для thread/sub-agent, сохраняющиеся через resume/restart. Польза: агент-оркестратор может быстро понять назначение и состояние каждого agent без перечитывания transcript. | Переносилась и усиливалась; первая metadata-only итерация перенесена в `fork/140` | `fork/140`, `fork/105`, `fork/106`, `fork/107`, `fork/111`, `fork/114`, `fork/116`, `fork/118`, `fork/130`, `chrome_plugin` |
| [Spawn agent cwd](spawn-agent-cwd.md) | Запуск sub-agent в явно выбранном рабочем каталоге. Польза: удобно делегировать задачи по разным проектам/worktrees из одного основного thread, а созданный в другой рабочей папке agent использует нативный функционал в директории, где был создан, что снижает риск изменений в некорректном месте. | Fork/140 first iteration implemented and verified locally; app-server/TUI/manual resume follow-ups remain documented | `fork/114`, `fork/118`, `fork/130`, `chrome_plugin`, `fork/140` |
| [Subagent policy and ownership](subagent-policy-and-ownership.md) | Ограничения прав sub-agent, allow/deny policies, запрет закрывать чужие branches. Польза: безопаснее multi-agent работа, меньше риска, что agent затронет не свой контекст. | Fork/140 ownership guards, native `read_only` workbench denial, explicit action allow/deny snapshots, app-server action-policy write, project role trust gating and read-only `.agents` metadata protection implemented; MCP allow/deny is intentionally managed through the generic native `[tool_selection]` surface instead of a dedicated MCP-only policy registry | `fork/105`, `fork/106`, `fork/107`, `fork/111`, `fork/colab-agents`, `fork/multi-agent`, `fork/140` |
| [Agent runtime limits](agent-runtime-limits.md) | Увеличенные лимиты depth, max threads и wait timeout. Польза: можно запускать более крупные multi-agent задачи без преждевременных ограничений. | Fork/140 defaults implemented and verified locally; upstream `rust-v0.140.0` covered this only partially | `fork/multi-agent`, `fork/106`, `fork/107`, `fork/114`, `fork/116`, `fork/130`, `chrome_plugin`, `fork/140` |
| [MCP on-demand discovery](mcp-on-demand-discovery.md) | Обнаружение MCP servers по требованию. Польза: MCP servers не нагружают контекст agent, если они не нужны agent. | Fork/140 first iteration implemented and verified locally; native `rust-v0.140.0` covered this only partially | `fork/106`, `fork/107`, `fork/140` |
| [run_skill_script tool](run-skill-script.md) | Tool для запуска scripts из skills через unified exec. Польза: увеличение эффективности и качества работы agent, так как skill может не только описывать действия, но и выполнять подготовленный helper. | Fork/140 first iteration implemented and verified locally; native `rust-v0.140.0` covered this only partially | `fork/multi-agent`, `fork/140` |
| [Provider-aware models and DeepSeek](provider-aware-models-deepseek.md) | Поддержка моделей разных providers, включая DeepSeek через Chat Completions. Польза: пользователь/agent выбирает не только model, но и корректный provider; `/model-providers` позволяет управлять видимостью providers и managed API keys. | Первая итерация перенесена в `fork/140`; исторически реализовано в `fork/130`/`chrome_plugin`; native `rust-v0.140.0` покрывает частично | `fork/140`, `feature/140/provider-model-catalog`, `fork/130`, `chrome_plugin` |

## Текущее состояние `fork/140`

Первая волна fork-возможностей закоммичена поверх `fork/140-upstream` в `fork/140`. Если новая возможность ещё находится в рабочем дереве, её нужно явно помечать как текущий dirty state в соответствующем feature doc.
